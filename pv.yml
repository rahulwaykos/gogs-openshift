apiVersion: v1
kind: PersistentVolume
metadata:
  name: nfs 
spec:
  capacity:
    storage: 2Gi 
  accessModes:
  - ReadWriteOnce 
  nfs: 
    path: /nfs-test 
    server:  
  persistentVolumeReclaimPolicy: Recycle 
